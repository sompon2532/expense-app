const teacher = (state = {}, action) => {
    switch (action.type) {
        case "ADD_TEACHER":
            return state;
            break;
        default:
            return state;
    }
}

export default teacher